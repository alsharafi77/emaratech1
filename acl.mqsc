SET CHLAUTH(CONNECT.ADMIN.CLIENT) TYPE(BLOCKUSER) USERLIST('nobody')
SET CHLAUTH(CONNECT.ADMIN.CLIENT) TYPE(ADDRESSMAP) ADDRESS(*) ACTION(REMOVE)
SET CHLAUTH(SYSTEM.ADMIN.SVRCONN) TYPE(ADDRESSMAP) ADDRESS(*) ACTION(REMOVE)
SET CHLAUTH(CONNECT.ADMIN.CLIENT) TYPE(ADDRESSMAP) ADDRESS(10.130.15.*) USERSRC(CHANNEL)
